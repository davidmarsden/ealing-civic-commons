# Follow button active-state labels

Follow controls keep the target visible after activation so visitors can tell what each active button represents.

Active labels:

- item: `✓ Following this story`
- source/place/topic: `✓ Following: {label}`

Inactive labels remain `+ Follow …`.

This is a presentation-only change. Stored follow identifiers and matching behaviour are unchanged.
