const $ = sel => document.querySelector(sel);
const esc = s => String(s ?? '').replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const fmtDate = iso => { if (!iso) return 'Date unavailable'; const d = new Date(iso); return new Intl.DateTimeFormat('en-GB',{day:'numeric',month:'long',year:'numeric'}).format(d); };
const pillClass = type => type === 'Official record' ? 'official' : type === 'Journalism / publishing' ? 'journalism' : type === 'Independent civic data / analysis' ? 'analysis' : 'organisation';

function routeInfo() { const parts = location.pathname.split('/').filter(Boolean); const slug = parts[0] === 'topics' ? parts[1]?.replace(/\.html$/,'') : null; return slug ? `topics/${slug}` : null; }
function renderProviders(items) { $('#topicProviders').innerHTML = (items || []).map(provider => `<div class="entity-provider"><strong>${provider.url ? `<a href="${esc(provider.url)}" target="_blank" rel="noopener noreferrer">${esc(provider.label || provider.name)}</a>` : esc(provider.label || provider.name)}</strong><span>${esc(provider.role || provider.bindingRole || 'Data provider')}</span></div>`).join(''); }
function renderList(sectionId, rootId, items, html) { if (!items?.length) return; $(sectionId).hidden = false; $(rootId).innerHTML = `<ul class="entity-list">${items.map(html).join('')}</ul>`; }
function renderHero(data) { const t=data.topic; document.title=`${t.name} — Civic Commons`; $('#topicStatus').hidden=true; $('#topicHero').hidden=false; $('#topicHero').innerHTML=`<h1>${esc(t.name)}</h1><p class="lede">${esc(t.description || 'A Civic Commons topic connecting current local material with reviewed civic memory and evidence.')}</p><div class="entity-kicker"><span class="tag">topic</span>${(t.aliases||[]).slice(0,5).map(a=>`<span class="tag">also: ${esc(a)}</span>`).join('')}</div><div class="entity-actions"><a href="#issuesSection">Ongoing issues ↓</a><a href="#entitiesSection">People & places ↓</a><a href="#sourcesSection">Evidence ↓</a><a href="#currentSection">Current Commons ↓</a></div>`; }
function renderStats(data) { const c=data.counts||{}; $('#topicStats').innerHTML=`<div class="entity-stat"><strong>${c.reporting||0}</strong><span>archive posts</span></div><div class="entity-stat"><strong>${c.entities||0}</strong><span>connected entities</span></div><div class="entity-stat"><strong>${c.sources||0}</strong><span>curated sources</span></div><div class="entity-stat"><strong>${c.issues||0}</strong><span>ongoing issues</span></div>`; }
function currentMatches(feed,data) { const topicNames=new Set(data.feedTopics||[]); const terms=[data.topic.name,...(data.topic.aliases||[])].filter(x=>String(x).length>=5).map(x=>String(x).toLowerCase()); return (feed?.items||[]).filter(item => (item.topics||[]).some(t=>topicNames.has(t)) || terms.some(term=>`${item.title||''} ${item.summary||''}`.toLowerCase().includes(term))).slice(0,16); }
function renderCurrent(items) { if(!items?.length)return; $('#currentSection').hidden=false; $('#currentItems').innerHTML=items.map(item=>`<article class="item"><div class="item-meta"><span class="source-pill ${pillClass(item.sourceClass)}">${esc(item.sourceClass)}</span><strong>${esc(item.source)}</strong><span>${esc(fmtDate(item.publishedAt))}</span></div><div><h3><a href="${esc(item.url)}" target="_blank" rel="noopener noreferrer">${esc(item.title)}</a></h3>${item.summary?`<p class="item-summary">${esc(item.summary)}</p>`:''}</div></article>`).join(''); }

async function load(){ const route=routeInfo(); if(!route){$('#topicStatus').innerHTML='<h1>Topic not found.</h1>';return;} try { const endpoint=new URL('/.netlify/functions/civic-topic',location.origin); endpoint.searchParams.set('route',route); const [r,fr]=await Promise.all([fetch(endpoint,{cache:'no-store'}),fetch('/.netlify/functions/feed',{cache:'no-store'}).catch(()=>null)]); if(!r.ok) throw new Error(`Topic HTTP ${r.status}`); const data=await r.json(); if(!data.matched)throw new Error(data.reason||'Topic not found'); renderHero(data);renderStats(data);renderProviders(data.providers);
renderList('#issuesSection','#topicIssues',data.issues,i=>`<li><h3><a href="/${esc(i.route)}">${esc(i.name)}</a></h3><p>${esc(i.description||'')}</p><span class="entity-meta">${esc(i.status||'ongoing')}</span></li>`);
renderList('#entitiesSection','#topicEntities',data.entities,e=>`<li><h3>${e.commonsRoute?`<a href="/${esc(e.commonsRoute)}">${esc(e.name)}</a>`:esc(e.name)}</h3><span class="entity-meta">${esc(e.type)} · ${e.count} archive post${e.count===1?'':'s'}</span></li>`);
renderList('#sourcesSection','#topicSources',data.sources,s=>`<li><h3>${s.url?`<a href="${esc(s.url)}" target="_blank" rel="noopener noreferrer">${esc(s.title)}</a>`:esc(s.title)}</h3><span class="entity-meta">${esc(s.publisher||'Source')}${s.date?` · ${esc(fmtDate(s.date))}`:''}</span></li>`);
renderList('#reportingSection','#topicReporting',data.reporting,p=>`<li><h3><a href="${esc(p.url)}" target="_blank" rel="noopener noreferrer">${esc(p.title)}</a></h3>${p.summary?`<p>${esc(p.summary)}</p>`:''}<span class="entity-meta">Southall Stories · ${esc(fmtDate(p.date))}</span></li>`);
if(fr?.ok)renderCurrent(currentMatches(await fr.json(),data));
} catch(error){$('#topicStatus').innerHTML='<h1>Civic topic temporarily unavailable.</h1><p>The topic service could not load this page.</p>';console.error(error);} }
load();
