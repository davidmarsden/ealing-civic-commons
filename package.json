{
  "name": "ealing-civic-commons-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "validate:contributions": "node scripts/validate-contributions.mjs",
    "inspect:ealing-data": "node scripts/ealing-data-catalog.mjs",
    "build": "npm run validate:contributions && mkdir -p dist/documents/content && cp -R public/. dist/ && cp docs/*.md dist/documents/content/"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.1",
    "fast-xml-parser": "^5.2.5"
  }
}
