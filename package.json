{
  "name": "ealing-civic-commons-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "mkdir -p dist/documents/content && cp -R public/. dist/ && cp docs/*.md dist/documents/content/"
  },
  "dependencies": {
    "fast-xml-parser": "^5.2.5"
  }
}
